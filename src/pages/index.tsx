import React, { useState } from 'react';

const Home = () => {
  const [productList, setProductList] = useState([]);

  return (
    <div>
      <p>Home de Prueba</p>
    </div>
  );
};

export default Home;
